name: Metrics

on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Base metrics
        if: ${{ success() || failure() }}
        uses: lowlighter/metrics@latest
        with:
          filename: metrics.base.svg
          token: ${{ secrets.METRICS_TOKEN }}
          user: bg-l2norm
          template: classic
          base: header, activity, community, repositories
          base_indepth: yes
          config_timezone: Asia/Kolkata
          config_display: columns
          retries: 3
          retries_delay: 300

      - name: Isometric calendar
        if: ${{ success() || failure() }}
        uses: lowlighter/metrics@latest
        with:
          filename: metrics.plugin.isocalendar.fullyear.svg
          token: ${{ secrets.METRICS_TOKEN }}
          user: bg-l2norm
          base: ""
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year
          config_timezone: Asia/Kolkata
          retries: 3
          retries_delay: 300

      - name: Languages details
        if: ${{ success() || failure() }}
        uses: lowlighter/metrics@latest
        with:
          filename: metrics.plugin.languages.details.svg
          token: ${{ secrets.METRICS_TOKEN }}
          user: bg-l2norm
          base: ""
          plugin_languages: yes
          plugin_languages_sections: most-used
          plugin_languages_limit: 8
          plugin_languages_details: bytes-size, percentage
          plugin_languages_ignored: >-
            html, css, tex, less, dockerfile, makefile, qmake, lex, cmake, shell, gnuplot
          config_timezone: Asia/Kolkata
          retries: 3
          retries_delay: 300

      - name: Featured repositories
        if: ${{ success() || failure() }}
        uses: lowlighter/metrics@latest
        with:
          filename: metrics.plugin.repositories.featured.svg
          token: ${{ secrets.METRICS_TOKEN }}
          user: bg-l2norm
          base: ""
          plugin_repositories: yes
          plugin_repositories_order: featured
          plugin_repositories_featured: Fuwa, MirrorAccent, awesome-ai-agent-papers--, Promptterfly, ChatEDU
          config_timezone: Asia/Kolkata
          retries: 3
          retries_delay: 300

      - name: Notable contributions
        if: ${{ success() || failure() }}
        uses: lowlighter/metrics@latest
        with:
          filename: metrics.plugin.notable.svg
          token: ${{ secrets.METRICS_TOKEN }}
          user: bg-l2norm
          template: classic
          base: ""
          plugin_notable: yes
          plugin_notable_from: organization
          plugin_notable_self: no
          plugin_notable_repositories: no
          plugin_notable_types: commit, pull_request, issue
          config_timezone: Asia/Kolkata
          retries: 3
          retries_delay: 300
          config_timezone: Asia/Kolkata
          retries: 3
          retries_delay: 300
